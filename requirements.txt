# Core runtime dependencies (Python 3.11+)

# Telegram bot
aiogram==3.17.0

# Web panel
fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.17

# Database
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0

# Redis (FSM + locks + throttling)
redis==5.2.0

# HTTP client (integrations, later phases)
httpx==0.28.0

# Scheduling (later phases; imported lazily)
apscheduler==3.10.4

# Config & validation
pydantic==2.10.3
pydantic-settings==2.6.1

# Auth / security
passlib[bcrypt]==1.7.4
# Pin bcrypt < 4.1: passlib 1.7.4 reads bcrypt.__about__ which newer bcrypt removed.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# Local/dev: SQLite async driver (for running without Docker/Postgres)
aiosqlite==0.20.0
